/**
 * Spinner Component
 *
 * Custom loading spinner matching Figma design with smooth animations.
 * Features a ring that rotates and pulses but never fully closes or completes.
 *
 * Inspired by One UI and Material M3 motion principles.
 */

export { default } from "./Spinner";
export { Spinner } from "./Spinner";
export type { SpinnerProps, SpinnerSize } from "./types";
